.row
  .col-lg-12
    .panel.panel-default
      .panel-heading= t(:funds_list)
      .panel-body
        .table-responsive
          %table.table
            %thead
              %tr
                %th= t(:name)
                %th= t(:amount)
                %th= t(:currency)
                %th= t(:description)
                %th= t(:actions)
            %tbody
              - @funds.each do |fund|
                %tr
                  %td= fund.name
                  %td= fund.amount
                  %td= fund.currency
                  %td= fund.description
                  %td
                    = link_to t(:edit), edit_fund_path(fund)
                    = link_to t(:destroy), fund, method: :delete, data: { confirm: t(:a_u_sure) }

.row
  .col-lg-12
    %h2.page-header= t(:cost_categories)
.row
  .col-lg-12
    %p.pull-right
      = link_to new_cost_category_path, class: 'btn btn-success' do
        %i.fa.fa-plus.fa-fw
        = t(:add_new_cost_category)

= cache 'last_cost.cost_categories.index' do
  = render 'all_month_limits'
  = render 'list'
